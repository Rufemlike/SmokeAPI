name: '🚧 Development Build'

on:
  push:
    branches:
      - '**' # This prevents dev builds on tag pushes

jobs:
  dev-build:
    name: '🚧 Development Build'
    uses: ./.github/workflows/matrix-build.yml
